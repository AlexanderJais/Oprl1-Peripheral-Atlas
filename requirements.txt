# Lower bounds, not exact pins: the analysis is not sensitive to patch versions,
# but it does depend on pandas 2+ groupby/reset_index semantics.
#
# Last verified end to end with:
#   python 3.11.2, pandas 3.0.5, numpy 2.4.6, scipy 1.16.3,
#   matplotlib 3.11.0, anndata 0.12.19, h5py 3.15.1, openpyxl 3.1.5
anndata>=0.10
h5py>=3.8
matplotlib>=3.7
numpy>=1.24
openpyxl>=3.1
pandas>=2.0
scipy>=1.10

# Manuscript PDF only: python3 paper/build_pdf.py
markdown>=3.5
weasyprint>=60

# Figures are typeset in Helvetica or a metric-compatible substitute, in the
# order src/atlas_style.py sets. Nimbus Sans is what the committed figures use;
# without it matplotlib falls through to Liberation Sans silently and every
# glyph in every figure moves. On Debian and Ubuntu: apt install fonts-urw-base35
#
# Tests only: python3 -m pytest tests -q
pytest>=7.0
